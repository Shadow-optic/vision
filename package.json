{
	"name": "vision",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "echo 'TypeScript Worker — wrangler compiles at deploy'",
		"check": "wrangler deploy --dry-run --outdir=/tmp/wrangler-dry-run",
		"deploy": "wrangler deploy"
	},
	"devDependencies": {
		"wrangler": "4.88.0"
	}
}
